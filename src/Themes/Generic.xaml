<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:FittsLaw.Controls"
    xmlns:conv="clr-namespace:FittsLaw.Converters">

    <SolidColorBrush x:Key="BrushButtonFill" Color="#D9F1FF" />
    <SolidColorBrush x:Key="BrushButtonFillHovered" Color="#BEE6FD" />
    <SolidColorBrush x:Key="BrushButtonFillPressed" Color="#B2E2FE" />
    <SolidColorBrush x:Key="BrushButtonFillDisabled" Color="#CCC" />
    <SolidColorBrush x:Key="BrushButtonBorder" Color="#BBB" />
    <SolidColorBrush x:Key="BrushButtonBorderHovered" Color="#89A" />
    <SolidColorBrush x:Key="BrushButtonTextDisabled" Color="#AAA" />

    <conv:ColorToBrush x:Key="ColorToBrushConverter"/>

    <Style TargetType="{x:Type ctrl:Hyperlink}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ctrl:Hyperlink}">
                    <TextBlock x:Name="PART_TextBlock">
                        <Hyperlink Name="PART_Link">
                            <TextBlock Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Hyperlink>
                    </TextBlock>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ctrl:ColorTextButton}">
        <Setter Property="MinWidth" Value="42"/>
        <Setter Property="Background" Value="{StaticResource BrushButtonFill}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushButtonBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Margin" Value="1,0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ctrl:ColorTextButton}">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5"
                            Margin="{TemplateBinding Margin}"
                            Padding="{TemplateBinding Padding}">
                        
                        <StackPanel Orientation="Vertical">

                            <!-- Top rectangle -->
                            <Rectangle x:Name="ColorRect"
                                       Height="25"
                                       Width="25"
                                       RadiusX="3"
                                       RadiusY="3"
                                       Fill="{Binding Color,
                                           RelativeSource={RelativeSource TemplatedParent},
                                           Converter={StaticResource ColorToBrushConverter}}"
                                       Stroke="Gray"/>

                            <!-- Bottom text -->
                            <TextBlock x:Name="TextLabel"
                                       Text="{Binding Text,
                                           RelativeSource={RelativeSource TemplatedParent}}"
                                       HorizontalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"
                                       Margin="0,6,0,0"/>
                        </StackPanel>

                    </Border>
                    
                    <!-- Visual States via Triggers -->
                    <ControlTemplate.Triggers>

                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource BrushButtonFillHovered}"/>
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{StaticResource BrushButtonBorderHovered}"/>
                            <Setter TargetName="ColorRect" Property="Opacity" Value="0.9"/>
                        </Trigger>

                        <!-- Pressed -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource BrushButtonFillPressed}"/>
                            <Setter TargetName="RootBorder" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.5"/>
                            <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource BrushButtonFillDisabled}"/>
                            <Setter TargetName="TextLabel" Property="Foreground" Value="{StaticResource BrushButtonTextDisabled}"/>
                        </Trigger>

                        <!-- Keyboard focus -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{StaticResource BrushButtonFillHovered}"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ToolbarIconButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="MinWidth" Value="42"/>
        <Setter Property="Padding" Value="2"/>
        <Setter Property="Margin" Value="2,0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushButtonBorder}"/>
        <Setter Property="Background" Value="{StaticResource BrushButtonFill}"/>

        <Style.Resources>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="5"/>
            </Style>
        </Style.Resources>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="#666"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#ACD7F0"/>
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
                <Setter Property="Background" Value="{StaticResource BrushButtonFillDisabled}"/>
                <Setter Property="Foreground" Value="{StaticResource BrushButtonTextDisabled}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="Background" Value="{StaticResource BrushButtonFillHovered}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushButtonBorderHovered}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
