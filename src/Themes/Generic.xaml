<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:FittsLaw.Controls"
    xmlns:conv="clr-namespace:FittsLaw.Converters">

    <conv:ColorToBrush x:Key="ColorToBrushConverter"/>

    <Style TargetType="{x:Type controls:ColorTextButton}">
        <Setter Property="MinWidth" Value="42"/>
        <Setter Property="BorderBrush" Value="#BBBBBB"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Margin" Value="1,0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ColorTextButton}">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5"
                            Margin="{TemplateBinding Margin}"
                            Padding="{TemplateBinding Padding}">
                        
                        <StackPanel Orientation="Vertical">

                            <!-- Top rectangle -->
                            <Rectangle x:Name="ColorRect"
                                       Height="25"
                                       Width="25"
                                       RadiusX="3"
                                       RadiusY="3"
                                       Fill="{Binding Color,
                                           RelativeSource={RelativeSource TemplatedParent},
                                           Converter={StaticResource ColorToBrushConverter}}"
                                       Stroke="Gray"/>

                            <!-- Bottom text -->
                            <TextBlock  x:Name="TextLabel"
                                        Text="{Binding Text,
                                           RelativeSource={RelativeSource TemplatedParent}}"
                                       HorizontalAlignment="Center"
                                       Margin="0,6,0,0"/>
                        </StackPanel>

                    </Border>
                    
                    <!-- Visual States via Triggers -->
                    <ControlTemplate.Triggers>

                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="#CFE6FD"/>
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="#666"/>
                            <Setter TargetName="ColorRect" Property="Opacity" Value="0.9"/>
                        </Trigger>

                        <!-- Pressed -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="#B2D8FE"/>
                            <Setter TargetName="RootBorder" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.5"/>
                            <Setter TargetName="TextLabel" Property="Foreground" Value="#AAAAAA"/>
                        </Trigger>

                        <!-- Keyboard focus -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="#68A8E9"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
